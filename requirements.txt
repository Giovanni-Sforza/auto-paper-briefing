# ============================================================
#  Auto-Paper-Briefing 依赖清单
# ============================================================

# 配置文件解析
PyYAML>=6.0

# SSL 证书（解决 macOS 打包后 CERTIFICATE_VERIFY_FAILED）
certifi>=2024.2.2

# PDF 文本提取（推荐，可选）
# 如无法安装，系统会自动降级到 pdfminer 或内置基础提取
PyMuPDF>=1.23.0

# PDF 文本提取（备选，任选其一）
# pdfminer.six>=20221105

# 注意：
# - Python 标准库已包含 urllib、json、xml 等，无需额外安装
# - arXiv API 通过官方 HTTP API 访问，无需第三方库
# - AI API 通过标准 HTTP POST 调用，无需 openai SDK（但可选用）
